PyYAML>=6.0        # rovers-protocol
inputs==0.5        # gamepads + keyboard
hidapi>=0.14.0     # SpaceMouse
digi-xbee==1.5.0   # XBee radio (optional; UDP simulation without it)

# tools/gps_reader.py additionally needs, on the Pi only:
#   Adafruit-Blinka